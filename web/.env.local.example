# Copy to .env.local for local dev. In Vercel, set these as Environment Variables.

# Public (safe in the browser)
NEXT_PUBLIC_SUPABASE_URL=https://hkopwyykqyknyujbsmzh.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-only — used by /api/crawl to trigger the GitHub Actions workflow.
GH_DISPATCH_TOKEN=your-fine-grained-pat-with-actions-read-write
GH_REPO=FredTSousa/CrawlerLigaLend-ria
GH_WORKFLOW=crawl.yml
GH_REF=main
