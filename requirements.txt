requests>=2.20
curl_cffi>=0.7
# IANA tz database so zoneinfo("Europe/Lisbon") resolves on all platforms
# (Windows + slim Linux images lack system tzdata) — used for fixture kickoff_at.
tzdata>=2024.1

# abola.py (player-ratings scraper)
beautifulsoup4>=4.12
lxml>=5.0
ddgs>=9.0
# googlesearch-python  # optional fallback search backend
